apiVersion: v1
kind: Service
metadata:
  name: whatsapp-webhook-service
  namespace: whatsapp-webhook
  labels:
    app: whatsapp-webhook
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: whatsapp-webhook 